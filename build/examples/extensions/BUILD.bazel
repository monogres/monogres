"""e2e test"""

load("@monogres//extensions/contrib:cfg.bzl", CFGS_CONTRIB = "CFGS")
load("@monogres//extensions/noset:cfg.bzl", CFG_NOSET = "CFG")
load("@monogres//extensions/sslutils:cfg.bzl", CFG_SSLUTILS = "CFG")
load(":test.bzl", "build_all_test")

[
    build_all_test(
        name = "contrib",
        cfg = cfg,
    )
    for cfg in CFGS_CONTRIB.values()
]

build_all_test(
    name = "noset",
    cfg = CFG_NOSET,
)

build_all_test(
    name = "sslutils",
    cfg = CFG_SSLUTILS,
)
