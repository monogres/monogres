"""e2e test"""

load("@monogres//extensions:all.bzl", "CFGS_ALL")
load(":test.bzl", "build_all_test")

[
    build_all_test(
        name = name,
        cfg = cfg,
    )
    for name, cfgs in CFGS_ALL.items()
    for cfg in cfgs
]
